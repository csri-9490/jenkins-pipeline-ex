@Library('roboshop') _

//log.info 'Starting'
//log.warning 'Nothing to do!'

pipeline {
    agent any

    environment {
        URL_TEST = "google.com"
    }

    parameters {
        string(name: 'MESSAGE', defaultValue: '', description: 'Message to print')
    }

    stages {
        stage('One') {
            steps {
                script {
                    log.info "${params.MESSAGE}"
                }
            }
        }
    }
}